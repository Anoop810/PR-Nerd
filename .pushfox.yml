provider: gemini
model: gemini-3.6-flash
fallback_models:
  - gemini-flash-latest
  - gemini-2.0-flash

review:
  max_iterations: 8
  severity_threshold: medium

paths:
  ignore:
    - node_modules
    - dist
    - build
    - coverage
    - .git
    - .pushfox
