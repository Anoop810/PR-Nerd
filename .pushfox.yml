provider: gemini
model: gemini-2.0-flash
fallback_models:
  - gemini-flash-latest
  - gemini-3.6-flash

review:
  max_iterations: 3
  severity_threshold: medium

paths:
  ignore:
    - node_modules
    - dist
    - build
    - coverage
    - .git
    - .pushfox
