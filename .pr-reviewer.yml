provider: gemini
model: gemini-2.5-flash

review:
  max_iterations: 8
  severity_threshold: medium

paths:
  ignore:
    - node_modules
    - dist
    - build
    - coverage
    - .git
    - .pr-review
